"use client";

import { motion } from "framer-motion";

export function HubDigitalShards({ shouldAnimate = true }: { shouldAnimate?: boolean }) {
  return (
    <div className="absolute inset-0 pointer-events-none opacity-90">

    </div>
  );
}
